APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8080

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=namvoy
DB_USER=namvoy
DB_PASS=change_me

SESSION_NAME=namvoy_session
# Set to true when serving over HTTPS
SESSION_SECURE=false

# Verification docs etc. Must be OUTSIDE the web root (public/).
# STORAGE_PATH=/absolute/path/outside/web/root  (default: ./storage/uploads)

# Payment providers (Step 5+) - leave blank for now
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
